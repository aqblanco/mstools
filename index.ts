/*import { Command } from "./src/Command.class";
import yargs from 'yargs';
import { AlbumFile } from "./src/AlbumFile.class";*/

// Clear console
/*console.clear();

let file = new AlbumFile("Accept - Symphonic Terror - Live at Wacken 2017 [HQ Ltd Bluray 2CD Digi] - 2018 OHM.7z");
console.log(file.getAlbum());*/

// Command
/*var cmd = yargs
.command('[options...]', 'Extract an album to the indicated path')*/

// -f parameter
/*cmd
.alias({file: 'f'})
.describe('f', 'Path to file');*/
/*cmd.option('f',{
  type: 'string',
  alias: 'file',
  describe: 'Path to file',
  demandOption: true
})*/

// -p parameter
/*cmd
.boolean('p')
.describe('p', 'Only set permissions');*/

/*cmd.option('p',{
  type: 'boolean',
  alias: 'permissions',
  describe: 'Only set permissions',
})

cmd.option('x',{
  type: 'string',
  alias: 'extractpath',
  describe: 'Set path where to extract manually',
  demandOption: true
})*/
// Process
//var argv = cmd.argv;

/*¡if (typeof argv.f === 'undefined') {
  console.log('File not specified');
} else {*/
  //console.log(argv.f);
//}